name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Rodando o build
        run: echo "Build concluído com sucesso"

  alert-discord:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Enviando alerta para Discord
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -H "Content-Type: application/json" \
          -d '{"content": "🚀 Um novo commit/merge foi realizado no projeto meu-projeto-devops!"}' \
          $DISCORD_WEBHOOK

test:
  needs: build
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v3
    - name: Instalando dependências
      run: pip install -r requirements.txt
    - name: Rodando testes unitários
      run: pytest

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Rodando o deploy
        run: echo "Deploy realizado com sucesso"
